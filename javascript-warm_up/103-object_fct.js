#!/usr/bin/node

const myObject = {
    type: 'object',
    value: 12
  };
number === myObject.value
  function incr (number, theFunction) {
    number += 1;
    theFunction(number);
  }
console.log(incr)

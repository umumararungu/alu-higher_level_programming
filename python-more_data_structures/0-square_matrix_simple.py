#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if matrix != 0:
        for i in matrix:
            square = (matrix[[i]] ** 2)
            return suqare
        else:
            return None
